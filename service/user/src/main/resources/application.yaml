server:
  port: 8082

spring:
  application:
    name: user-service
  cloud:
    compatibility-verifier:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/